spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dhq?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=UTC
    #url: jdbc:mysql://127.0.0.1:3306/dhq?useUnicode=true&useSSL=false&characterEncoding=utf8
    username: root
    password: 123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update #新增数据库连接必备的参数
  mvc:
    view:
      prefix: /WEB-INF/views/
      suffix: .jsp

server:
  port: 8080
  tomcat:
    uri-encoding: UTF-8
  servlet:
    session:
      timeout: 30
name: sdfhjsakf